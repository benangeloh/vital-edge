[project]
name = "fleetview-contracts"
version = "1.0.0"
description = "Format wire bersama antara Edge Agent dan Central Platform"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.11"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fleetview_contracts"]
